import { ArrowRight, CheckCircle, Github, Loader2, LogOut, Settings, User } from "lucide-react"

export const Icons = {
  arrowRight: ArrowRight,
  check: CheckCircle,
  github: Github,
  spinner: Loader2,
  user: User,
  logOut: LogOut,
  settings: Settings,
}

